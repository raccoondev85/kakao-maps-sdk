export declare class Viewpoint {
    pan: number;
    tilt: number;
    zoom: number;
    panoId: number;
    private object;
    constructor(pan: number, tilt: number, zoom: number, panoId?: number);
}
